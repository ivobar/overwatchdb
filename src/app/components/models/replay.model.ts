import { SafeUrl } from "@angular/platform-browser";

export class ReplayModel {
    constructor(
        public video: string,
        public thumbnail: string,
        public title: string
    ) { }
}